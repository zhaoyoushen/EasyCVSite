# PersonalHomepage 用户认证流程详解

## 🔧 技术架构

### 1. 核心技术栈
- **ASP.NET Core Identity**: 提供完整的用户认证和授权框架
- **Entity Framework Core**: 数据持久化和用户数据管理
- **SQL Server**: 用户数据存储
- **Cookie Authentication**: 会话管理

### 2. 数据模型设计

**ApplicationUser (扩展IdentityUser)**:
- `DisplayName`: 用户显示名称
- `CustomUrl`: 个性化URL
- `CreatedAt`: 创建时间
- `LastLoginAt`: 最后登录时间
- `IsActive`: 账户状态
- `Profile`: 关联的用户配置文件

**UserProfile**:
- 存储用户的个人主页配置
- 通过外键与ApplicationUser一对一关联
- 包含JSON格式的配置数据

## 🔐 认证流程详解

### 1. 用户注册流程

**技术实现**:
1. **前端验证**: `RegisterViewModel`进行数据验证
   - 邮箱格式验证
   - 密码强度要求(至少6位，包含大小写字母和数字)
   - 确认密码匹配

2. **后端处理**: `AccountController.Register`
   ```csharp
   var user = new ApplicationUser
   {
       UserName = model.Email,
       Email = model.Email,
       DisplayName = model.DisplayName,
       CreatedAt = DateTime.UtcNow
   };
   var result = await _userManager.CreateAsync(user, model.Password);
   ```

3. **自动登录**: 注册成功后立即调用`SignInAsync`
4. **重定向**: 跳转到Dashboard仪表板

**业务逻辑**:
- 新用户注册即可获得个人主页空间
- 系统自动分配用户ID和基础配置
- 支持邮箱作为唯一标识符

### 2. 用户登录流程

**技术实现**:
1. **身份验证**: `SignInManager.PasswordSignInAsync`
2. **会话管理**: Cookie有效期30天，支持滑动过期
3. **登录记录**: 更新`LastLoginAt`时间戳
4. **返回URL处理**: 支持登录后返回原访问页面

**安全特性**:
- 账户锁定: 5次失败尝试后锁定5分钟
- 记住我功能: 可选的持久化登录
- 本地URL验证: 防止开放重定向攻击

### 3. 授权控制

**技术实现**:
- `[Authorize]`特性保护需要认证的控制器
- `User.Identity.IsAuthenticated`检查认证状态
- 中间件管道: `UseAuthentication()` → `UseAuthorization()`

**访问控制**:
- 未认证用户: 自动重定向到登录页面
- 已认证用户: 可访问Dashboard和个人配置
- 公开页面: 无需认证即可访问

## 🏢 业务流程

### 1. 用户生命周期管理

**注册阶段**:
- 用户提供邮箱、显示名称和密码
- 系统创建用户账户和关联的配置文件
- 自动生成个人主页空间

**使用阶段**:
- 用户登录后进入Dashboard
- 可以编辑个人信息、技能、项目等
- 支持自定义URL和主题样式
- 可以发布/取消发布个人主页

**数据管理**:
- 用户配置以JSON格式存储
- 支持实时保存和预览
- 访问统计和更新时间跟踪

### 2. 个人主页发布机制

**发布控制**:
- 用户可选择是否公开个人主页
- 支持自定义URL访问路径
- 提供多种主题样式选择

**访问统计**:
- 记录页面访问次数
- 跟踪最后更新时间
- 提供数据分析支持

## 🛡️ 安全机制

### 1. 密码安全
- 最小长度6位
- 必须包含大小写字母和数字
- 使用ASP.NET Core Identity的哈希算法

### 2. 会话安全
- Cookie加密存储
- 支持滑动过期
- 安全的注销机制

### 3. 数据保护
- 用户数据隔离
- 外键约束保证数据完整性
- 级联删除保护

## 📊 系统优势

1. **标准化**: 基于ASP.NET Core Identity的成熟框架
2. **可扩展**: 支持角色管理和权限扩展
3. **安全性**: 内置多层安全防护机制
4. **用户体验**: 流畅的注册登录流程
5. **数据完整性**: 完善的数据关系和约束

## 📝 核心代码示例

### Program.cs - Identity配置
```csharp
// Add Identity services
builder.Services.AddIdentity<ApplicationUser, IdentityRole>(options =>
{
    // Password settings
    options.Password.RequireDigit = true;
    options.Password.RequireLowercase = true;
    options.Password.RequireNonAlphanumeric = false;
    options.Password.RequireUppercase = true;
    options.Password.RequiredLength = 6;
    options.Password.RequiredUniqueChars = 1;
    
    // Lockout settings
    options.Lockout.DefaultLockoutTimeSpan = TimeSpan.FromMinutes(5);
    options.Lockout.MaxFailedAccessAttempts = 5;
    options.Lockout.AllowedForNewUsers = true;
    
    // User settings
    options.User.RequireUniqueEmail = true;
})
.AddEntityFrameworkStores<ApplicationDbContext>()
.AddDefaultTokenProviders();
```

### AccountController - 登录处理
```csharp
[HttpPost]
public async Task<IActionResult> Login(LoginViewModel model, string? returnUrl = null)
{
    if (ModelState.IsValid)
    {
        var result = await _signInManager.PasswordSignInAsync(
            model.Email, model.Password, model.RememberMe, lockoutOnFailure: false);
        
        if (result.Succeeded)
        {
            // 更新最后登录时间
            var user = await _userManager.FindByEmailAsync(model.Email);
            if (user != null)
            {
                user.LastLoginAt = DateTime.UtcNow;
                await _userManager.UpdateAsync(user);
            }
            
            return RedirectToLocal(returnUrl);
        }
    }
    return View(model);
}
```

### DashboardController - 授权访问
```csharp
[Authorize]
public class DashboardController : Controller
{
    public async Task<IActionResult> Index()
    {
        var user = await _userManager.GetUserAsync(User);
        if (user == null)
        {
            return RedirectToAction("Login", "Account");
        }
        
        // 获取用户配置和数据
        var profile = await _userConfigService.GetUserProfileAsync(user.Id);
        var configuration = await _userConfigService.GetUserConfigurationAsync(user.Id);
        
        return View(viewModel);
    }
}
```

---

这套认证系统为PersonalHomepage项目提供了完整、安全、易用的用户管理解决方案，支持用户从注册到个人主页发布的全流程管理。